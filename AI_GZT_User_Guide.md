# AI智能工作台 Windows版 使用说明

## 版本信息
- 软件版本:v1.0 正式版
- 适用系统:Windows 10 / Windows 11 64位
- 更新日期:2026年08月01日
- 内置运行环境:无需额外安装Python，核心功能开箱即用

---

## 一、软件安装
### 1.1 获取安装包
请从官方发布渠道获取最新版Windows安装包，文件格式为 `.exe` 安装程序。

### 1.2 安装步骤
#### 方式一:EXE安装版（推荐普通用户）
1. 双击运行 `AI_GZT_Setup_v1.0.exe` 安装程序
2. 选择安装目录（默认路径:`C:\Users\用户名\AppData\Local\Programs`，可自行选择安装目录，建议不要安装在C盘系统保护目录例如C:\Program Files）
3. 勾选「添加桌面快捷方式」「创建开始菜单文件夹」
4. 点击「下一步」等待安装完成，全程约1-2分钟

#### 方式二:ZIP绿色免安装版
1. 将压缩包解压到非系统盘的任意目录（例如 `D:\AI_GZT`）
2. 解压路径不要包含中文、特殊字符或空格，避免启动异常
3. 双击根目录下的 `AI_GZT.exe` 即可直接运行，无需写入注册表

### 1.3 启动方式
1. **桌面快捷方式**:安装完成后双击桌面「AI_GZT」图标启动
2. **开始菜单**:在开始菜单找到「AI_GZT」文件夹点击启动
3. **绿色版**:直接双击解压目录下的 `AI_GZT.exe`
4. 首次启动会自动释放运行依赖，可能需要等待10-30秒，请耐心等待

---

## 二、首次启动说明
本软件为完全免费的开源软件，**无需激活、无需注册码、无任何功能限制**，启动即可使用全部功能:
1. 双击桌面「AI_GZT」图标启动程序
2. 首次启动会自动释放运行依赖，可能需要等待10-30秒，请耐心等待
3. 启动后进入工作台，打开「系统设置」，填写你的大模型 API Key（支持火山方舟/DeepSeek/通义千问等OpenAI兼容接口）即可开始对话
4. 仓库路径、插件市场、定时任务等功能均开箱即用，无需额外配置

> 💡 提示:本软件源码完全开源（Apache 2.0），可自由使用、修改、分发。免费安装包未经代码签名，首次运行时 Windows 可能弹出 SmartScreen 提示，按 README 中「未签名安装包如何跳过 SmartScreen」章节操作即可。

---

## 三、首次使用向导
### 3.1 端口自动检测
## 二、首次使用向导
### 2.1 端口自动检测
程序启动时会自动检测端口占用情况:
- 默认使用8000端口，如果端口被占用会自动顺延使用8001、8002等可用端口
- 实际访问地址会在启动控制台和系统托盘菜单中显示

### 2.2 配置 模型API密钥配置 参数设置 环境检测 路径设置
可设置参数说明:
#### 2.2.1 模型配置
  1. 打开Web界面，点击左侧菜单栏「配置」按钮
  2. 找到「大模型配置」板块，选择你使用的模型服务商（OpenAI/豆包/通义千问等）
  3. 输入对应的API Key、API接口地址（快捷入口按钮点击后打开火山方舟可以注册账号开通模型，按需选择，复制相应的API Key、API）
  4. 超时参数设置，模型连接超时设置（无需修改）
  5. 温度参数设置，数值低回复和操作越严谨，数值高则越发散（根据需求调整，正常范围为0.5-0.8）
  6. 回复上限参数设置，默认为10，代表每轮对话AI模型的输出次数和工具调用次数（按需调整，根据模型的上下文窗口大小设置） 
  7. 流式输出开关，默认开（无需修改）  
  8. 点击「保存配置」
  9. 配置完成后即可正常使用AI对话功能
  
#### 2.2.2 记忆与上下文配置（向量检索环境检测如果没有安装相关依赖则记忆检索功能失效，可把上下文轮数的参数数值适当调高些，AI的记忆和操作会更准确些）
  1. 上下文轮数参数设置，默认5（代表相应的项目记忆保留在上下文中的对话轮数，根据需求调整）
  2. 记忆检索数参数设置，默认4（每轮对话中检索之前对话中相应的记忆，需要安装相应的依赖，如果没有安装则无效）
  3. 相似度阈值参数设置，默认0.9（记忆检索相关设置数值越高则相关程度越高建议0.8-1.3之间）
  4. 召回触发阈值参数设置，默认2（记忆检索相关设置，代表对话历史记录超过两轮后才触发记忆检索，无需修改）
  5. 固定提示1，内容为工作台给AI上下文中注入的工具调用规则和参数说明（可微调）
  6. 固定提示2，内容为AI上下文中注入补充规则说明（根据需求可微调）
  7. 固定提示3，暂时为空可根据需求进行添加约束规则(添加完成保存后会实时注入AI上下文中) 
  8. 点击「保存配置」保存后立刻生效
  
#### 2.2.3 向量检索环境检测
  1. 检测电脑的环境依赖支持，如未安装相应的依赖则向量记忆检索和RAG代码检索功能不能使用
  2. 可根据提示找到相应的脚本文件运行安装环境和依赖
  
#### 2.2.4 存储设置
  1. 仓库存储路径（可自行选择仓库路径）
  2. 下载保存路径（可自行选择保存路径）
  
#### 2.2.5 运行设置
  1. 显示调试控制台开关，默认关闭（可选择打开保存后重启工作台，可以显示控制台）

#### 2.2.6 安全设置 - 禁止操作列表内的文件目录AI无法执行操作
  1. 点击按钮选择文件或文件夹加入列表，保存配置即时生效
---

## 三、核心功能说明
### 3.1 AI对话
- 支持多轮上下文对话、流式输出、对话历史自动保存
- 重启工作台或者电脑上下文对话不丢失
- 支持上传文件、图片进行多模态对话
- 支持自定义系统提示词、切换不同模型、配置页面支持自定义的参数设置
- 悬浮窗模式和工作台操作模式可以自由切换

### 3.2 工作台与文件管理
- 内置文件仓库，支持上传、分类、预览各类文档
- 支持代码文件在线编辑、一键保存
- 支持文件版本自动备份，可随时恢复历史版本
- 所有文件均保存在本地，不上传到第三方服务器

### 3.3 插件扩展
- 内置官方插件，可一键安装各类扩展功能
- 已内置插件:
  - AI图片生成:文生图、图生图、专业卡片生成（需要自行开通相应的生图模型才能使用）
  - 信息采集:联网搜索、网页内容提取
  - 视频生成:文本/图片生成AI视频（需要自行开通相应的视频生成模型才能使用）
  - 进程监控:本地进程安全检测、恶意程序拦截
  - 编辑工具补充插件：解决编辑时因内容包含content:/target_block:等参数关键词导致的截断错改
- 支持第三方自定义插件上传安装

### 3.4 定时任务
- 支持可视化配置定时任务，可设置定时执行脚本、采集信息、备份文件
- 支持Cron表达式自定义复杂调度规则
- 任务执行日志完整记录，执行结果可推送通知

### 3.5 本地RAG知识库（可选功能）
- 支持导入本地文档构建专属知识库
- 支持代码库语义检索，开发场景快速定位代码片段
- 对话时自动关联知识库内容，回答更精准
- ⚠️ 该功能需要安装额外外部依赖，详见第四章说明

### 3.6 其他功能
- 自动同步监控:文件变更自动备份、多端同步
- 网页插件市场:可扩展更多第三方功能

---

## 四、可选外部依赖安装（RAG本地知识库）
### 4.1 功能说明
本地RAG向量检索功能依赖PyTorch、FAISS等机器学习库，为避免安装包体积过大（完整依赖会增加约3-4G体积），默认没有内置到安装包中:
- ✅ 不安装依赖:所有核心功能（对话、文件管理、插件、定时任务等）完全正常使用，RAG检索自动降级为关键词匹配模式，不影响基础使用
- ✅ 安装依赖后:启用完整语义向量检索，知识库召回准确率提升80%以上，支持代码语义搜索、本地文档问答

### 4.2 环境要求
- 必须安装 **Python 3.11 64位版本**（推荐3.11）
- Python下载地址:https://www.python.org/downloads/release/python-3119/
- 安装时务必勾选「Add Python to PATH」选项

### 4.3 安装步骤
1. 确认Python安装成功:打开cmd命令提示符，输入 `python --version`，显示版本号为3.11.x即正常
2. 执行以下pip命令安装依赖（推荐使用国内镜像源速度更快）:
```bash
pip install torch==2.2.2 faiss-cpu==1.8.0 sentence-transformers==2.6.1 transformers==4.44.2 numpy==1.26.4 -i https://pypi.tuna.tsinghua.edu.cn/simple
```
3. 等待安装完成，全程根据网络情况约5-15分钟
4. 重启AI智能助手程序，程序会自动检测已安装的依赖并加载，无需任何手动配置
5. 进入「系统设置」-「向量检索环境检测」- 向量检索功能已启用，记忆检索开启，RAG功能开启

### 4.4 常见问题
- 提示「Python版本不匹配」:请卸载当前Python，安装3.11版本，重启程序后即可识别
- 安装速度慢/超时:请使用上面命令中的清华镜像源，不要使用默认官方源
- 安装后仍提示未检测到依赖:确认Python和程序都是64位版本，重启电脑后再试
- 显存不足报错:CPU版本也可正常运行，仅首次加载模型较慢，无需额外配置

---

## 五、常见问题排查
### 5.1 启动失败，双击exe无反应
1. 检查安装/解压路径是否包含中文、空格或特殊字符，建议移动到纯英文路径
2. 检查是否被杀毒软件拦截，将程序目录添加到杀毒软件白名单
3. 右键点击exe，选择「以管理员身份运行」
4. 查看程序目录下的 `logs` 文件夹，查看最新错误日志定位问题

### 5.2 对话提示API连接失败
1. 检查网络是否正常，能否正常访问API服务商接口
2. 确认API Key是否正确、是否有余额
3. 私有部署场景确认API接口地址填写正确，末尾不要加 `/v1` 后缀
4. 检查系统代理是否影响API请求，可在设置中配置代理地址

### 5.3 外部依赖不识别
1. 确认Python版本为3.10/3.11 64位，且已添加到系统PATH
2. 在cmd中执行 `pip list` 确认所有依赖包已正确安装
3. 重启AI_GZT程序，必要时重启电脑
4. 如果使用了虚拟环境，请在系统全局Python环境中安装依赖

### 5.4 如何查看运行日志
所有运行日志均保存在程序目录下的 `logs` 文件夹中:
- `app.log`:主程序运行日志
- `error.log`:错误异常日志
- `plugin.log`:插件运行日志
遇到问题时可提供对应日志给技术支持排查。

---

## 六、卸载与数据备份

### 6.1 卸载软件
1. 关闭正在运行的AI智能助手（右键系统托盘图标选择「退出」）
2. 打开Windows「设置」-「应用」-「安装的应用」
3. 找到「AI智能助手」点击「卸载」，按照提示完成卸载
4. 如需完全清除数据，手动删除安装目录下的 `data` 文件夹即可

### 6.2 升级版本
升级时直接覆盖安装到原目录，或解压新版文件覆盖旧版文件即可，所有配置和用户数据会自动保留，无需重新配置。

---

## 技术支持
如遇到本文档未覆盖的问题，可通过以下方式获取支持:
1. 查看项目官方Wiki和常见问题库
2. 在项目GitHub/Gitee仓库提交Issue
3. 加入官方用户群反馈问题