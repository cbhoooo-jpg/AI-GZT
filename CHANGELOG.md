# 更新日志（Changelog）

本项目所有显著变更均记录在本文件中。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### 变更

- 项目手册代码元数据提取升级为多格式统一提取架构（`project_manual_manager.py`）:提取器注册表按扩展名分发，Python 沿用 AST 精确解析；新增 HTML 内嵌 JS（Vue methods/computed 一层直接子键 + 顶层 function）、独立 JS/TS、PowerShell、BAT/CMD、Inno Setup 五类提取器
- 修复 HTML 方法提取两处缺陷:① 方法键固定缩进上限（2~16 空格）误杀深缩进页面，`web_log.html` 的 10 个方法曾全部漏提，改为花括号配平取块体后只收相对 option 键最浅一层的直接子键；② 注释/字符串掩码状态机不识别正则字面量，含引号的正则（如 `inputPattern: /^[^\\/:*?"<>|]+$/`）会导致其后到文件尾全部被掩码，`web_file_repo.html` 曾漏提 3 个方法，状态机新增正则体与字符类 `[...]` 识别并按前置 token 区分正则与除号
- 统一防护:第三方压缩库三重排除（`*.min.js` / static 等目录 / 512KB 体积）、UTF-8→GBK 编码兜底、单文件条目上限 80 条、解析失败 24 小时缓存不阻塞手册同步
- 2026-09-15 真机全量同步复测通过:`web_log.html` 10 个方法、`web_file_repo.html` 20 个方法全部正确入库，其余页面与 PS1/BAT/ISS 提取结果零回归

## [1.0.1] - 2026-09-15

### 修复

- 修复 Windows 版首次安装后默认仓库被创建到 C 盘用户文档目录（C:/Users/用户/Documents/AI仓库文件夹）的问题:全新安装、配置缺失或配置项为空时，默认仓库统一创建到 D:/AI仓库文件夹；Mac/Linux 仍沿用用户文档目录
- 插件开发默认目录不再硬编码 C 盘文档路径，改为复用统一的默认仓库路径函数
- 修复 repository_path 配置为空串时被解析为当前工作目录的隐患，load_config 与 init_repository_path 双重兜底默认值

### 兼容性说明

- 已自定义过仓库路径的存量用户升级后路径保持不变，不会被覆盖；如需迁移到 D 盘，可在设置页修改路径并勾选迁移文件

## [1.0] - 2026 年

首个正式版本。

### 新增

- Windows 桌面工作台:PySide6 桌面端 + Web 管理界面，支持全屏工作台与迷你悬浮窗双模式自由切换
- 模型接入:支持云端多模态模型 API 可视化配置（保存后即刻生效、无需重启），同时支持本地离线模型
- RAG 私有知识库:基于 faiss + gte-small-zh 中文向量模型（512 维），支持代码库索引与对话记忆召回；向量依赖缺失时自动优雅降级，不影响其他功能
- 9 个官方插件:
  - AI 图片生成（文生图 / 图生图 / 多图融合）
  - AI 视频生成（支持图生视频）
  - 网页信息采集
  - 文件格式转换（图片 / 文档 / 表格）
  - 本地进程监控与风险分级处置
  - 屏幕录制（H.264 MP4，支持暂停续录与变速压缩）
  - 企业微信远程通道
  - RAG 增强与本地知识库
  - 安全文件编辑
- 插件开发体系:标准化工具调用协议，提供三类插件模板（AIGC 生成类 / 信息采集查询类 / 本地操作类）
- 文件仓库:项目文件集中管理，支持上传、分类、在线预览与代码编辑
- 定时任务:可视化新建任务，支持 Cron 表达式
- 备份管理与完整操作日志审计:AI 的文件操作与命令执行全程留痕、可回溯
- 工程化打包:PyInstaller + Inno Setup 一键打包，安装包内置 Python 运行环境与全部依赖，开箱即用

### 安全

- 本地隐私优先:对话记忆、知识库、配置与 API 密钥默认仅保存在用户本机，不中转、不收集
- 文件操作内置系统关键目录禁止列表
- 进程监控内置系统关键进程保护名单
- 插件按需授权，远程通道令牌仅存储于本地配置

[1.0]: https://gitee.com/chen-bohan3000/ai-gzt