# 常见问题（FAQ）

本页汇总新用户最常遇到的问题。更完整的排障说明请阅读软件内附的《使用说明》（`AI_GZT_User_Guide.md`）第六章。

## 一、安装类

### 1. 双击安装包弹出蓝色「Windows 已保护你的电脑」怎么办？

安装包尚未购买代码签名证书，SmartScreen 对所有未签名软件都会提示，**不代表软件有风险**。点击「更多信息」→「仍要运行」即可，随后 UAC 黄窗点「是」。完整 4 步图文教程见 README:[未签名安装包如何跳过 SmartScreen](README.md#未签名安装包如何跳过-smartscreen安装包用户必读)。

### 2. 安装版还需要自己装 Python 吗？

不需要。`.exe` 安装包（约 199 MB）已内置 Python 运行环境与全部核心依赖，安装后开箱即用。只有 **源码运行**（`git clone` 后 `python main.py`）才需要自行安装 Python 3.11 与依赖。

### 3. 启动失败 / 双击没反应？

- 安装或解压路径不要包含中文、空格、特殊字符（绿色版尤其注意）
- 检查杀毒软件是否拦截，可将程序目录加入白名单
- 查看程序目录 `logs/` 下的 `app.log`、`error.log` 定位问题
- 详见《使用说明》6.1 节

## 二、模型与 API 类

### 4. 如何配置大模型 API？

打开 Web 界面 → 左侧「配置」→「大模型配置」板块:① 选择模型服务商（OpenAI / 豆包 / 通义千问等 OpenAI 兼容接口）；② 填入 API Key 与 API 接口地址；③ 点击「保存配置」，**即刻生效无需重启**。API Key 仅保存在你本机，请求直接发往服务商，本项目不中转、不收集。

### 5. 对话提示「API 连接失败」？

依次排查:网络能否访问服务商接口 → API Key 是否正确、账户是否有余额 → 私有部署时接口地址末尾**不要加 `/v1`** → 系统代理是否影响请求（可在设置中配置代理地址）。

### 6. 发图片 / 截图后报错？

默认需使用**多模态模型**。若你配置的是纯文本模型，图片消息会报错，在设置中切换为多模态模型即可。

## 三、RAG 知识库与记忆类

### 7. 提示向量检索 / RAG 功能不可用？

RAG 语义检索依赖 PyTorch、faiss-cpu 等约 3.5 GB 的可选依赖，安装包默认未内置。**不装也不影响对话、插件等核心功能**，记忆检索会自动降级。需要完整语义检索时:

- 源码用户:双击根目录 `Python和依赖安装.bat` 一键安装
- 安装版用户:按《使用说明》第五章手动安装可选依赖（需 Python 3.11 64 位）

### 8. 源码运行时 `gte-small-zh/pytorch_model.bin` 去哪下载？

受 Git 仓库体积限制，该权重文件（约 58 MB）未纳入仓库，缺失时 RAG 自动降级、其他功能不受影响。下载与放置步骤见 README:[源码运行补充:向量模型文件](README.md#源码运行补充向量模型文件)（ModelScope 网页直下或 `modelscope download` 命令）。

## 四、数据与隐私类

### 9. 我的对话记录和 API Key 会被上传吗？

不会。对话记忆、知识库索引、配置文件全部保存在本地；云端模型请求由你的电脑直发服务商。详见 README「第三方服务与数据隐私」章节。

### 10. 如何备份 / 迁移我的数据？

使用工作台内置的「备份管理」功能；手动迁移时备份程序目录下的 `data` 文件夹与 `config.json` 即可。

---

以上未覆盖的问题，欢迎在 [Gitee Issue](https://gitee.com/chen-bohan3000/ai-gzt/issues)（国内推荐）或 [GitHub Issues](https://github.com/cbhoooo-jpg/AI-GZT/issues) 反馈。