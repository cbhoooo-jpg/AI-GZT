@echo off
chcp 65001 >nul
rem ============================================================
rem  本脚本已升级收口（2026-09-14）
rem  旧版仅单向推送 GitHub 且硬编码路径已失效；
rem  双机同步统一改用「同步代码.bat」（Gitee 中转，可选顺带推 GitHub）。
rem ============================================================
cd /d "%~dp0"
echo 旧的「推送到GitHub.bat」已升级，即将启动统一同步脚本...
echo   - 关键同步走 Gitee（国内快），先 pull 再 push
echo   - 双击会询问是否顺带推 GitHub；也可直接用「同步代码.bat all」
echo.
call "同步代码.bat"