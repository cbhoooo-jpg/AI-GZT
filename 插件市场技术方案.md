# 云端插件市场技术方案

> 本文件承载插件市场的详细设计与变更记录；项目手册第四章第 8 条只保留一行摘要指向本文件。
> 最后更新:2026-09-15

## 一、方案选型

采用 **方案 C:整仓 archive 分发（MVP）**。

- 插件集仓库（Gitee 公开库 `ai-gzt-plugins`）根目录维护 `market_manifest.json` 清单；
- 安装时任一插件都下载 Gitee 自动生成的整仓 zip，解压时只取目标插件子目录；
- 优点:零发版操作、永远最新、匿名可访问（仓库已公开，无需 token）；
- 后续正式发版可平滑切换为"每插件独立 Release 包":manifest 结构不变，仅把插件级 `download_url` 填上即可，后端代码零改动。

## 二、清单文件 market_manifest.json

位于插件集仓库根目录，已推送 Gitee（commit aefe3b0）。

- 顶层字段:`schema_version` / `market_name` / `distribution` / `archive_url` / `updated_at` / `plugins[]`；
- `archive_url`:`https://gitee.com/chen-bohan3000/ai-gzt-plugins/repository/archive/main.zip`；
- `plugins[]` 每项:`plugin_id`、`name`、`version`、`category`、`author`、`description`、`permissions`、`min_app_version`；
- 当前收录 9 款插件:channel_wecom、file_converter、image_generator、info_collector、process_monitor、rag_enhance、safe_file_editor、screen_recorder、shipin_shengcheng。

## 三、后端接口（api_server.py）

### 1. 配置读取 `get_plugin_market_config()`
- 读 `config.json` 的 `plugin_market` 节点，支持 `enabled`、`manifest_url`；
- 默认清单地址为 Gitee raw 地址，可配置覆盖，不硬编码；
- `config.example.json` 已补 `plugin_market` 示例。

### 2. `GET /api/plugin/market/list?force=false`
- 拉取远程清单，5 分钟内存缓存，`force=true` 跳过缓存；
- 与本地已安装插件按 `plugin_id` 合并，输出三态:`not_installed` / `update_available`（附 `installed_version`）/ `installed`；
- 阻塞 IO 用 `asyncio.to_thread` 包裹，不卡事件循环；
- 拉取失败返回 502，只影响市场 Tab，不影响已安装列表。

### 3. `POST /api/plugin/market/install`
- 请求体:`{"plugin_id": "xxx"}`，独立于旧 `/api/plugin/install`；
- 流程:流式下载整仓 zip（200MB 上限）→ 安全解压到临时目录 → 只取目标插件子目录 → 校验 plugin.json 必填字段与 ID 一致 → 备份并保留旧版 `user_config.json` → 移入 plugins 目录 → `load_plugins()` 热重载确认 → 临时文件无条件清理。

## 四、安全解压函数 `_safe_extract_zip()`

云端安装与本地 zip 上传安装共用，修复了原 `extractall` 的两个老隐患:

1. **zip-slip 防护**:逐个成员校验解压后绝对路径必须落在目标目录内，拦截 `../../` 路径穿越；
2. **自动剥公共顶层目录**:zip 内所有文件共享同一层顶层目录时（如 Gitee 整仓包的 `ai-gzt-plugins-main/`）自动剥除；
3. 本地 zip 上传接口同步切换:改为读 plugin.json 确定真实插件 ID（不再用 zip 文件名当目录名），更新安装同样保留 `user_config.json`。

## 五、前端（web_plugin_market.html）

- 可安装 Tab 新增:市场名称/清单更新时间信息栏、纯文字「刷新市场列表」按钮（无图标）、加载态、错误重试区；
- `loadMarketPlugins(force)`:进 Tab 自动拉取，成功走前端缓存，刷新强制 force；失败只显示错误区，不影响已安装 Tab；
- 卡片三态:未安装（安装）/ 可更新（更新到 vX，当前 vY，更新前弹确认）/ 已安装（灰色禁用）；
- `installMarketPlugin()`:安装中按钮 loading 防重复点击，成功后联动刷新已安装 Tab 与市场三态。

## 六、网络策略变更（2026-09-15 用户拍板）

**取消全部域名白名单，允许访问任意网站:**

- 删除市场白名单 `MARKET_ALLOWED_DOWNLOAD_DOMAINS`，校验函数改为 `_is_valid_http_url()`，仅校验 http/https 协议（拦截 `file://`、`ftp://` 等）；
- 删除终端 curl/wget 的 `ALLOWED_DOWNLOAD_DOMAINS` 域名拦截块，AI 命令行可访问任意网站；
- 保留措施:200MB 下载上限、zip-slip 防护、plugin.json 校验、危险命令关键字拦截（rm/format/rd 等）。

## 七、真机测试清单

1. 重启服务 → 可安装 Tab 显示 9 个插件卡片；
2. 安装一个插件 → 按钮 loading → 成功后卡片变"已安装（最新）"，已安装 Tab 同步出现；
3. 刷新市场列表强制拉取；断网或改错 manifest_url 时显示错误区且已安装 Tab 不受影响；
4. 更新场景:手动降低本地插件版本号后验证"可更新"按钮与配置保留；
5. 白名单验证:让 AI 执行访问非白名单域名的 curl 命令，确认不再被拦截。

> 注:开发终端网络受工具层策略限制，Gitee 连通性需在真实运行环境验证。

## 八、下载通道变更（2026-09-15 真机实测修复）

**问题根因**:真机卸载后重装报 `File is not a zip file`。实测 Gitee 整仓 archive 端点（`repository/archive/main.zip`）对非浏览器会话不返回 zip——带浏览器 Cookie+Referer 仍返回 405/HTML 页面，后端把 HTML 存成 .zip 导致解压失败。清单走 raw 通道正常，故列表正常、仅安装失败。

**修复方案**（api_server.py，零新依赖、不依赖 git）:

- 新增 `_parse_repo_info_from_manifest()`:从 manifest_url 自动推导仓库地址/分支，支持清单显式 `repo_url/ref` 覆盖，不硬编码；
- 新增 `_download_market_plugin_files()`:匿名 **contents API 递归列出目标插件子目录** → **raw 地址逐文件下载** → 临时目录还原结构；
- 三重防护:单插件文件数 ≤500（`MARKET_PLUGIN_MAX_FILES`）、总体积 ≤200MB、路径穿越校验；
- `_install_market_plugin()` 改双通道:插件配独立 `download_url` 时走 zip（新增 **PK 魔数校验**，再遇 HTML 错误页明确报错），缺省走逐文件通道；plugin.json 校验、user_config 备份、热重载逻辑不变。

**真机结论**:2026-09-15 用户真机实测，插件市场自行安装成功，本功能验收通过。